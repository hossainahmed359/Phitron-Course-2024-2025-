
// semester-1/recap-week/sum-of-consecutive-numbers.txt
// G. Summation from 1 to N
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    
    long long int sum = 0;
    
    // WRONG APPROACH
    // for(int i = 1; i <= n; i++)
    // {
    //     sum += i;
    // }
    
    // CORRECT -> ((n+1) X n )/2
    
    sum = (long long int)n * (n + 1) / 2;
    
    printf("%lld\n", sum);

    
    
    return 0;
}